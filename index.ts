'use strict';

import getConsolidatedData from './getConsolidatedData';
import performSetup from './performSetup';

export default {
    getConsolidatedData: getConsolidatedData,
    performSetup: performSetup
};